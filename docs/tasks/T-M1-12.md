# T-M1-12: エントリーポイント統合

| 項目 | 内容 |
|------|------|
| マイルストーン | M1 |
| 依存 | T-M1-9, T-M1-10, T-M1-11 |
| 要件ID | FR-001, FR-005 |

## 説明
全モジュールを結合し、Bot の起動からメッセージ送受信までの E2E フローを動作させる `index.ts` を完成させる。

## 作業内容
1. 各モジュールの初期化順序を整理
2. Discord クライアント起動
3. メインセッション（ccbot-main）の自動作成
4. OutputWatcher の起動とイベントハンドラ接続
5. E2E 動作確認（Discord メッセージ --> Claude 応答 --> Discord 投稿）

## タスクのチェックリスト
- [x] `bun run src/index.ts` で Bot が起動する
- [x] Discord メッセージが Claude CLI に送信される
- [x] Claude の応答が Discord に投稿される
- [x] メインセッションが自動起動する

## 実装結果メモ
- 起動フロー: Discord クライアント初期化 -> メインセッション作成 -> SessionStore 登録 -> OutputWatcher 起動 -> Discord ログイン
- OutputWatcher の `"output"` イベントで `text` タイプのイベントのみ Discord に投稿（idle / session_end は現時点では無視）
- `main()` 関数のエラーは `process.exit(1)` で終了
